import '../app/globals.css' // Import the global CSS styles
import 'tailwindcss/tailwind.css' // Import Tailwind CSS styles

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
